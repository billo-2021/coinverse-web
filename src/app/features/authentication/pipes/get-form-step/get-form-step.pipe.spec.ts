import { GetFormStepPipe } from './get-form-step.pipe';

describe('GetFormStepsPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFormStepPipe();
    expect(pipe).toBeTruthy();
  });
});
