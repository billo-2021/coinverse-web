import { PolymorphicDirective } from './outlet.directive';

describe('PolymorphicDirective', () => {
  it('should create an instance', () => {
    const directive = new PolymorphicDirective();
    expect(directive).toBeTruthy();
  });
});
