import { Pagination } from '../types';

export const PAGINATION: Pagination = { page: 0, size: 5 };
export const TOTAL_ITEMS = 1;

export const PAGE_OPTIONS = { count: 0, total: TOTAL_ITEMS, data: [] };
