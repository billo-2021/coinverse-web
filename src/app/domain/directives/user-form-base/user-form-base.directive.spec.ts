import { UserFormBaseDirective } from './user-form-base.directive';

describe('UserFormBaseDirective', () => {
  it('should create an instance', () => {
    const directive = new UserFormBaseDirective();
    expect(directive).toBeTruthy();
  });
});
